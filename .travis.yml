language: php

php:
  - 7.3

services:
  docker

install:
  - docker --version
  - php --version
  - curl -sS https://getcomposer.org/installer | php --
  - php composer.phar install --no-interaction --no-dev
  - npm install
  - npm run production
  - ./dev.sh up -d

script:
  - npm run test
